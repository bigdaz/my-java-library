node {
    stage('scm') {
        echo "${scm.repositories[0].remote}"
        //checkout scm
        // Made a change    
    }
    stage('Hello') {
        echo 'Hello World'
    }
    stage('Goodbye') {
        echo 'Goodbye cruel world!!!'
    }
}
